stages:
- name: deploy
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: myweb:v1
      pushRemote: true
      registry: harbor.big2.com
timeout: 60
notification: {}
